CREATE TABLE [Imp].[FarmaciaPopularCoPay] (
    [Medication]  VARCHAR (MAX) NOT NULL,
    [Usage]       VARCHAR (MAX) NOT NULL,
    [Amount]      VARCHAR (MAX) NOT NULL,
    [Referencia]  VARCHAR (MAX) NOT NULL,
    [PrecoMaximo] VARCHAR (MAX) NOT NULL
);

